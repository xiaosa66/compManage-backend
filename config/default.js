const config = {
    // 启动端口
    port: 3000,
  
    // 数据库配置
    database: {
      DATABASE: 'compManage',
      USERNAME: 'root',
      PASSWORD: 'xiaosa98',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
  
  module.exports = config